# CLAUDE.md

> **Scope: this folder only.** Parent context: {{PARENT}}/CLAUDE.md. Repo rules: {{ROOT}}/CLAUDE.md.
> Docs here override the root's where present; missing docs inherit the parent (nearest file wins).

## What this is
{{WHAT}} <!-- one paragraph, what IS — not what should be -->

## Key files
```
TODO(verify): path ← what it is
```

## Local gotchas
1. <!-- bolded lead-in, the failure mode, the real bug in parentheses. Delete if none yet. -->

## Divergences from the root standard (honest state — don't pretend otherwise)
- none recorded (verified {{DATE}})
