# FROZEN

Surfaces no agent edits without the human saying so, per line. Checked by `lock.sh frozen <path>`.

Format: `- <glob> — why, and who froze it (date)`

- Example: src/legacy/billing/** — hand-tuned, replacement planned (2026-09-12)
