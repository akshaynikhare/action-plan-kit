# DECISIONS

Cross-cycle memory. Newest first. Read via `recall.sh` — never whole.
An entry exists so a future cycle does not "fix" a deliberate choice back.

Entry format:

## YYYY-MM-DD — <decision as an imperative sentence>

**Do not <the thing a helpful future agent will try>.**
Why, with the mechanism, in 2–4 sentences.
**Pinned by** `<test file path>` — what it asserts.
**Adjacent, deliberately NOT changed:** <optional>

---
