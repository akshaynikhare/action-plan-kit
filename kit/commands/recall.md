---
description: Slice DECISIONS/PATTERNS/FROZEN for a query — never read the registers whole.
---

Run `bash .actionplan/scripts/recall.sh "<query>"` (or `--titles` for the overview) and relay the blocks.
Reading any register file in full with Read is the named anti-pattern — do not do it.
