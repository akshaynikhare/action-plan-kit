---
description: Record a deliberate choice in DECISIONS.md so no future cycle "fixes" it back.
---

Read `.actionplan/AP.md`. Usage: `/ap:decide <title>`.

1. `recall.sh "<key words>"` first — near-duplicate found? If this entry INVERTS a prior rule for a different
   context, the body must carry a contrast table (context A → rule, context B → opposite rule, why both are right).
2. Refuse to write without **Pinned by** — a real test path that exists on disk and asserts the behaviour.
3. Prepend the entry (newest first) using the format in `DECISIONS.md`'s header: date — imperative title,
   bolded do-not, the mechanism why, Pinned by, optional "Adjacent, deliberately NOT changed".
4. Show the entry. **Stop.**
