---
description: The board + velocity + the Mirror. Numbers speak; nothing to show → MIRROR clean.
---

Run `node .actionplan/scripts/status.cjs` and relay it verbatim — the Mirror lines are not to be softened.
Add nothing except, when a Mirror line names a fix (steal a lock, split a plan), the exact command for it.
